(function () {

    const vkService = window.app.xhrService;

    /**
     * @param mountNode
     * @constructor
     */
    function Chat(mountNode) {

    }
    
    app.chatComponent = Chat;
})();
